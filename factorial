#include<stdio.h>
#include<conio.h>

int main()
{
	int input,i,fact;
	printf("what value you want to find factorial?\n");
	scanf("%d",&input);
	for(i=1; i<=input; i++)
	{
		fact = fact*i;
	}
	printf("factorial of %d is %d",input,fact);
}
