#include<stdio.h>
#include<conio.h>

int main()
{
	int k;
	int all[k], i;
	int sum=0;
	scanf("%d",&k);
	for(i=0;i<k;i++)
	{
		printf("enter %d element\n", i);
		scanf("%d", &all[i]);
		
	}
	for(i=0; i<k; i++)
	{
		printf("%d+", all[i]);
		sum +=all[i];
	}
	printf("=%d",sum);
	
}
